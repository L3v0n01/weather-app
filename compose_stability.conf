// Compose Stability Configuration
//
// Domain models from :core:model are used directly in UI state across feature modules.
// This module is pure JVM with no Compose dependency, so @Immutable/@Stable annotations
// cannot be applied. Instead, we declare stability here.
//
// All listed classes are data classes with only val fields (primitives, String, enums,
// ImmutableList) — they satisfy Compose stability contract.
//
// For larger projects, consider a domain-to-UI mapping layer with @Immutable annotations.

com.la.weather.core.model.weather.*
com.la.weather.core.model.location.*
com.la.weather.core.model.settings.*
